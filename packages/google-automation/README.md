# @key-set/google-automation

Visible, user-authorized Google Cloud Console automation. The browser never
accepts credentials from Keyset; the user performs Google login and any 2FA.
If the Console UI cannot be verified, the operation stops before application
files are changed.
