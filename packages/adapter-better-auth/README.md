# @keyset/adapter-better-auth

Safe, idempotent Better Auth source mutations for Google and GitHub OAuth.

The adapter only edits recognized configuration files under the supplied project root, preserves existing providers, and reads credentials from environment variables. It never receives or prints raw client secrets.

See [Better Auth](../../docs/BETTER-AUTH.md).
