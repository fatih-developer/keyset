# @keyset/adapter-authjs

Safe, idempotent Auth.js source mutations for Google and GitHub OAuth.

The adapter preserves existing provider imports and entries, refuses ambiguous provider arrays, and references environment variables rather than embedding credentials.

See [Auth.js](../../docs/AUTHJS.md).
