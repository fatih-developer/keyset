# ADR 0001 — Core independence

Status: Accepted

Keyset Core must not depend on an LLM, MCP, IDE, coding agent, or browser automation implementation.

CLI, MCP, SDK, skills, providers, and auth adapters are replaceable boundaries around the core workflow.
